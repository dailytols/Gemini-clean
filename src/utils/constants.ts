export const SUPPORTED_MIME_TYPES = [
  'image/png',
  'image/jpeg',
  'image/jpg',
  'image/webp'
];

export const MAX_FILE_SIZE_MB = 15; // Limit to 15MB for local memory safety

export const DEFAULT_CONFIDENCE_THRESHOLD = 0.70;

export const BATCH_SIZES = [1, 5, 10, 20];
